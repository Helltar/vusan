.agents/
.antigravitycli/
.claude/
.codex/
.gradle/
.idea/
.kotlin/

build/
data/
deploy/
node_modules/
notes/

CLAUDE.local.md
deno.lock
!sandbox/deno.lock

# claude code sandbox masks home dotfiles with /dev/null stubs, visible only inside its sandboxed commands
.bash_profile
.bashrc
.gitconfig
.gitmodules
.mcp.json
.profile
.ripgreprc
.vscode
.zprofile
.zshrc

# each service keeps its environment beside its own compose file; only the examples are tracked
.env
!.env.example

# runtime directories, each beside the compose file that mounts it
services/sites/data/
services/sites/certs/
