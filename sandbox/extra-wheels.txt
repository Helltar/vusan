# Pure-Python wheels not shipped in the Pyodide distribution. Downloaded at
# build time (Dockerfile `wheels` stage) and unpacked onto sys.path in worker.ts
# so document creation/editing works fully offline. Pin exact versions for
# reproducible images. Their compiled/in-distribution dependencies (lxml,
# typing-extensions, Pillow, fonttools) are loaded via packages.ts.
python-docx==1.2.0
fpdf2==2.8.7
defusedxml==0.7.1
pypdf==6.14.2
