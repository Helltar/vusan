services:
  vusan:
    image: ghcr.io/helltar/vusan:latest
    container_name: vusan-container
    restart: unless-stopped
    env_file: .env
    networks:
      - default      # outbound internet (Telegram, LLM, web tools)
      - sandbox-net  # reach the sandbox service
    volumes:
      - vusan_data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # Opt-in code sandbox. Enable with: docker compose --profile sandbox up -d
  # (and set SANDBOX_URL=http://sandbox:8080 in .env). Runs untrusted Python on
  # an internal-only network with no secrets, no internet, and no host mounts.
  sandbox:
    image: ghcr.io/helltar/vusan-sandbox:latest
    container_name: vusan-sandbox
    profiles: ["sandbox"]
    restart: unless-stopped
    networks:
      - sandbox-net
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    # Several heavy Pyodide/WASM workers can be alive at once (warm pool + the
    # in-flight run + a respawning replacement), so give some headroom over 1g.
    mem_limit: 2g
    # Cap CPU so a busy-loop (bounded only by the run timeout) cannot peg every
    # host core for the full SANDBOX_TIMEOUT_SECONDS window.
    cpus: "2.0"
    tmpfs:
      - /tmp
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  sandbox-net:
    internal: true  # no route to the internet; egress for untrusted code is impossible

volumes:
  vusan_data:
